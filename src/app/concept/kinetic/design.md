# Kinetic Field

这是一个独立的、动效优先的 Ascend 前端概念，不引用 `/proposals/*` 的组件、样式或视觉规则。

- 核心隐喻：学习不是堆积任务，而是在知识场中持续制造动量。
- 三个状态：今日脉冲、知识星图、复习回声，通过共享元素和空间转场切换。
- 动效职责：入场建立层级、轨道表达连接、进度变化表达状态、指针视差表达空间、完成动画提供即时反馈。
- 可交互内容：视图切换、任务完成、知识节点选择、命令面板、沉浸式专注计时器。
- 可访问性：完整键盘入口，支持 `prefers-reduced-motion`，信息不依赖动画才能读取。
